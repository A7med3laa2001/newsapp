import 'package:flutter/material.dart';

class SettingesScreen extends StatelessWidget {
  const SettingesScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Settinges Screen",style: TextStyle(fontSize: 20.0,fontWeight: FontWeight.bold),));
  }
}
